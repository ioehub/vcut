#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../packages/audio-editor/node_modules/vite-node/vite-node.mjs" "$@"
  ret=$?
else 
  node  "$basedir/../../packages/audio-editor/node_modules/vite-node/vite-node.mjs" "$@"
  ret=$?
fi
exit $ret
